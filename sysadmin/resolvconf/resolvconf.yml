---
- name: set single-request dns options
  hosts: all
  remote_user: "{{ remote_user }}"

  tasks:
  - name: System | Single-Request set in Resolvconf
    sudo: yes
    lineinfile: dest=/etc/resolv.conf line="options single-request"
